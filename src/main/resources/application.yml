spring:
  application:
    name: ms-items-svc
  profiles:
    active: default
    
server:
  port: 8002

eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}} 
  client:
    service-url:
      defaultZone: http://eurekasrv.dconcesionaria.lomosrv.com:8761/eureka
      
sbms:
  api:
    products:
      endpoint: http://products.dconcesionaria.lomosrv.com
      resource: /sbms/products